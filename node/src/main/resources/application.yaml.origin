server:
  port:

spring:
  kafka:
    bootstrap-servers:
    topics:
      answer-topic:
      text-update-topic:
  datasource:
    url:
    username:
    password:
  jpa:
    generate-ddl:

headhunter:
  token:
  user-agent:
  endpoint:
    searchForVacancy:
notifier:
  period: