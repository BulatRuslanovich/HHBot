server:
  port:

spring:
  kafka:
    bootstrap-servers:
    topics:
      answer-topic:
      text-update-topic:
      callback-query-update-topic:
  datasource:
    url:
    username:
    password:
  jpa:
    generate-ddl:

headhunter:
  token:
  user-agent:
  endpoint:
    searchForVacancy:

notifier:
  period:

keyboard:
  noExperience:
  oneToThreeYears:
  threeToSixYears:
  moreThanSixYears:
  noFilter: